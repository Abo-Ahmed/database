create or replace TRIGGER UPDATE_CMN_SNOOZE
   AFTER UPDATE  ON CUSTOMERS_MOBILE_NUMBERS FOR EACH ROW


DECLARE
   V_REAL_DUE TIMESTAMP(6);
   
   
BEGIN
   
IF ((:NEW.CMN_SNOOZE_TO_DATE <> :OLD.CMN_SNOOZE_TO_DATE  AND :NEW.CMN_SNOOZE_TO_DATE IS NOT NULL) OR
    (:NEW.CMN_BLOCK_TO_TIME <> :OLD.CMN_BLOCK_TO_TIME  AND :NEW.CMN_BLOCK_TO_TIME IS NOT NULL))THEN
    
    
    V_REAL_DUE := GET_CONTACT_DUE_TO_TIMESTAMP (P_SNOOZE_TIME      => :NEW.CMN_SNOOZE_TO_DATE,
                                                P_BLOCK_TIME       => :NEW.CMN_BLOCK_TO_TIME);
                                      
  
   FOR I IN (SELECT MN_RELATION_TYPE_CODE, MN_RELATION_VALUE, MN_DUE_TO_TIMESTAMP
             FROM MESSAGE_NOTIFICATIONS
             WHERE MN_NOTIFICATION_CHANNEL = 'SMS'
             AND MN_RELATION_TYPE_CODE = :NEW.CMN_RELATION_TYPE_CODE
             AND MN_RELATION_VALUE = :NEW.CMN_RELATION_VALUE
             AND MN_CONTACT = :NEW.CMN_MOBILE_NUMBER
             AND (MN_DUE_TO_TIMESTAMP = NULL
              OR MN_DUE_TO_TIMESTAMP < V_REAL_DUE )) LOOP
              
  
      UPDATE MESSAGE_NOTIFICATIONS
      SET MN_DUE_TO_TIMESTAMP = V_REAL_DUE
      WHERE MN_NOTIFICATION_CHANNEL = 'SMS'
      AND MN_CONTACT = :NEW.CMN_MOBILE_NUMBER
      AND MN_RELATION_TYPE_CODE = I.MN_RELATION_TYPE_CODE
      AND MN_RELATION_VALUE = I.MN_RELATION_VALUE;
      
  END LOOP;
  
  
   FOR Y IN (SELECT MNB_RELATION_TYPE_CODE, MNB_RELATION_VALUE, MNB_DUE_TO_TIMESTAMP
             FROM MESSAGE_NOTIFICATIONS_BULK
             WHERE MNB_NOTIFICATION_CHANNEL = 'SMS'
             AND MNB_RELATION_TYPE_CODE = :NEW.CMN_RELATION_TYPE_CODE
             AND MNB_RELATION_VALUE = :NEW.CMN_RELATION_VALUE
             AND MNB_CONTACT = :NEW.CMN_MOBILE_NUMBER
             AND (MNB_DUE_TO_TIMESTAMP = NULL
              OR MNB_DUE_TO_TIMESTAMP < V_REAL_DUE )) LOOP
              
  
      UPDATE MESSAGE_NOTIFICATIONS_BULK
      SET MNB_DUE_TO_TIMESTAMP = V_REAL_DUE
      WHERE MNB_NOTIFICATION_CHANNEL = 'SMS'
      AND MNB_CONTACT = :NEW.CMN_MOBILE_NUMBER
      AND MNB_RELATION_TYPE_CODE = Y.MNB_RELATION_TYPE_CODE
      AND MNB_RELATION_VALUE = Y.MNB_RELATION_VALUE;
      
  END LOOP;
  
END IF;
 
      
END;