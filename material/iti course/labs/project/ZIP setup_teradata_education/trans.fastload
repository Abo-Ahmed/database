SESSIONS 8;

/* LOGON Teradata_Education, teradata; */

RUN logon.fastload;

DATABASE au;
/*
CREATE TABLE trans,
     NO FALLBACK,
     NO BEFORE JOURNAL, 
     NO AFTER JOURNAL
     (
      TRANS_NUMBER INTEGER,
      TRANS_DATE DATE,
      ACCOUNT_NUMBER INTEGER,
      TRANS_ID CHAR(4),
      TRANS_AMOUNT DECIMAL(10,2))
PRIMARY INDEX( ACCOUNT_NUMBER );
*/
BEGIN LOADING au.trans
ERRORFILES trans_E1, trans_E2
INDICATORS
;

DEFINE FILE = trans.dat
;

INSERT trans.*;

END LOADING;


QUIT;